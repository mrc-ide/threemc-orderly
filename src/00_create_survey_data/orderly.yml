script: script.R

artefacts:
  - data:
      description: Survey circumcision data
      filenames: 
      - survey_circumcision.csv.gz
  - data:
      description: Survey circumcision data
      filenames: 
      - survey_clusters.csv.gz
  - data:
      description: Survey individuals data
      filenames: 
      - survey_individuals.csv.gz
   - data:
      description: Area hierarchies and boundaries
      filenames: 
      - areas.geojson 

displayname: Survey and area hierarchy/boundary data
    
packages:
  - dplyr
  - sf
  - readr
